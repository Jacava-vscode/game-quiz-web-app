// Profile page for full-access mode (no user/token logic)
const ProfilePage = () => (
  <div style={{padding:40}}>
    <h2>Profile</h2>
    <p>All features are open access. No login required.</p>
  </div>
)
export default ProfilePage
